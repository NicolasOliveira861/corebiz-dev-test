import { styled } from '../../../stitches.config';

export const Container = styled('div', {
  width: '100%',
  maxWidth: '1250px',
  margin: '0 auto',

  '@max_sm': {
    margin: '0 0.4375rem',
  },
});
