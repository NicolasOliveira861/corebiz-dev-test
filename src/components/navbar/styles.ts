import { styled } from '../../../stitches.config';

export const Container = styled('nav', {
  display: 'flex',
  padding: '1rem 0 1.4375rem',
  justifyContent: 'center',
  alignItems: 'center',
});

export const Href = styled('a', {
  margin: '0 1.3125rem 0 2.6875rem',
});
